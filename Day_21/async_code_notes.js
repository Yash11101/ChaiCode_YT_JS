/*
JS is synchronous lang
JS is single threaded / hence  relatively slow

blocking code-->blocks flow of code // Stops execution until task finishes
Non-blocking code-->doesnot blocks flow of code // Doesn’t stop execution

JS executes program line by line
each operation waits for previous to finish.

web APIs handle asunc tasks-->it stores completed async task in task queue-->then moved to call stack when its empty

*/
